server:
  port: 8082

spring:
  application:
    name: auth-service
  
  datasource:
    #Cambien aqui por su BD
    url: jdbc:mysql://localhost:3306/DWB2026_2
    username: dwb
    password: Dwb2026_Spring!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Swagger
springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: method
    doc-expansion: none

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
